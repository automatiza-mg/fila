{{define "content"}}
  <main class="flex min-h-svh flex-col items-center justify-center">
    <div>
      <p class="text-2xl font-bold">{{.Status}} - {{.Title}}</p>
      <p class="text-muted-foreground">{{.Description}}</p>
    </div>
  </main>
{{end}}
